# Lucent 页面设计说明

## 页面访问

开发服务器已启动：**http://localhost:5173/**

---

## 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│                      控制栏 (60px)                            │
│  ┌──────────────┬──────────────────────┬──────────────────┐ │
│  │ Logo + 标题   │   代理状态控制         │   主题 + 刷新    │ │
│  └──────────────┴──────────────────────┴──────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┬─────────────────────────────────────────┐ │
│  │              │                                          │ │
│  │              │         详情面板 (5个Tab)                 │ │
│  │              │                                          │ │
│  │  日志列表     │  ┌──────────────────────────────────┐ │ │
│  │              │  │ Tab: Request | Response | ...     │ │ │
│  │  - Main/Sub  │  ├──────────────────────────────────┤ │ │
│  │  - 时间/耗时  │  │                                  │ │ │
│  │  - Token数   │  │        JSON / 内容显示            │ │ │
│  │              │  │                                  │ │ │
│  │              │  └──────────────────────────────────┘ │ │
│  │              │                                          │ │
│  └──────────────┴─────────────────────────────────────────┘ │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 1. 控制栏 (Control Bar)

### 左侧：品牌信息
```
📦 Lucent
   AI Agent 代理服务器
```

### 中间：代理状态控制
```
代理状态: ● 已启用  [启用/禁用按钮]
           ○ 已禁用

状态指示:
  ● 绿色 = 代理运行中
  ○ 灰色 = 代理未启用
```

### 右侧：工具按钮
```
[🌙/☀️] 主题切换  [🔄 刷新]  [其他设置]
```

---

## 2. 日志列表面板 (左侧)

### 布局
```
┌─────────────────┐
│ 通信记录 (12)    │
├─────────────────┤
│                 │
│ ✓ Main          │ ← 状态图标 + Agent 类型 + 模型
│   claude-3-5     │
│   10:30:15 · 2.3s │ ← 时间 + 耗时 + Token
│                 │
│ 🔄 Sub  [plan]  │ ← 进行中的请求
│   claude-3-haiku│
│   10:29:45      │
│                 │
│ ✗ Main          │ ← 错误的请求
│   gpt-4         │
│   10:28:00 · Error
│                 │
└─────────────────┘
```

### 列表项信息

| 元素 | 说明 |
|------|------|
| **图标** | ✓ 成功 / 🔄 进行中 / ✗ 失败 |
| **Agent 类型** | Main (主) / Sub (辅) |
| **子类型标签** | plan / search / bash / workflow |
| **提供商标签** | OpenAI (绿) / Claude (蓝) |
| **时间** | HH:MM:SS |
| **耗时** | X.XXs / XXXms |
| **Token** | input + output 总数 |

### 交互
- 点击列表项 → 右侧显示详情
- 选中项高亮显示

---

## 3. 详情面板 (右侧)

### Tab 导航
```
[📄 Request] [✓ Response] [💾 KV-Cache] [💬 Context] [ℹ️ Meta]
```

### 各 Tab 内容

#### Tab 1: Request
显示完整的 API 请求
```json
{
  "method": "POST",
  "url": "https://api.anthropic.com/v1/messages",
  "headers": {
    "x-api-key": "sk-ant-***",
    "anthropic-version": "2023-06-01"
  },
  "body": {
    "model": "claude-3-5-sonnet-20241022",
    "messages": [...],
    "tools": [...],
    "system": "...",
    "stream": true
  }
}
```

#### Tab 2: Response
显示完整的 API 响应
```json
{
  "status": 200,
  "statusText": "OK",
  "headers": {...},
  "body": {
    "id": "msg_xxx",
    "type": "message",
    "role": "assistant",
    "content": [...],
    "usage": {
      "input_tokens": 1234,
      "output_tokens": 567
    }
  }
}
```

#### Tab 3: KV-Cache
显示缓存相关信息
```
┌─────────────────────┐
│ 缓存命中             │
│ 85.3%                │
├─────────────────────┤
│ 读取字节             │
│ 12,345               │
├─────────────────────┤
│ 写入字节             │
│ 2,048                │
├─────────────────────┤
│ 缓存内容             │
│ system prompt...     │
└─────────────────────┘
```

#### Tab 4: Context
显示对话上下文
```json
{
  "messages": [
    {"role": "user", "content": "..."},
    {"role": "assistant", "content": "..."}
  ],
  "system": "You are Claude...",
  "tools": [...]
}
```

#### Tab 5: Meta
显示元数据信息
```
┌─────────────────────┐
│ Agent 类型           │
│ 主 Agent             │
├─────────────────────┤
│ 子类型               │
│ 无                   │
├─────────────────────┤
│ 模型                 │
│ claude-3-5-sonnet    │
├─────────────────────┤
│ 提供商               │
│ claude               │
├─────────────────────┤
│ 耗时                 │
│ 2345ms               │
├─────────────────────┤
│ 流式                 │
│ 是                   │
├─────────────────────┤
│ Token 使用           │
│ 1234 + 567           │
└─────────────────────┘
```

---

## 4. 主题样式

### 浅色主题 (默认)
```css
背景: #fafafa (layout) / #ffffff (container)
文字: #000000e6 (主要) / #000000a6 (次要)
边框: #e0e0e0
主色: #1890ff (蓝色)
```

### 深色主题
```css
背景: #0a0a0a (layout) / #111111 (container)
文字: #ffffffe6 (主要) / #ffffffa6 (次要)
边框: #2a2a2a
主色: #1668dc (蓝色)
```

---

## 5. 响应式设计

### 断点
- **桌面** (>1200px): 左右分屏
- **平板** (768-1200px): 可调整分割比例
- **手机** (<768px): 单列 + 抽屉导航

---

## 6. 状态指示

### 代理状态
| 状态 | 颜色 | 图标 |
|------|------|------|
| 已启用 | 绿色 | ● |
| 已禁用 | 灰色 | ○ |

### 请求状态
| 状态 | 图标 | 颜色 |
|------|------|------|
| 成功 | ✓ | 绿色 |
| 进行中 | 🔄 | 蓝色 (旋转) |
| 失败 | ✗ | 红色 |

---

## 7. 设计原则

1. **信息密度**: 左侧列表紧凑，右侧详情完整
2. **视觉层次**: 重要信息突出，次要信息弱化
3. **即时反馈**: 操作后立即有视觉反馈
4. **清晰导航**: Tab 明确，易于切换
5. **数据优先**: 内容展示为主，装饰为辅

---

现在访问 **http://localhost:5173/** 即可查看页面效果！
